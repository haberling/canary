# Guide

The concepts behind Canary, not just the commands. If you want a quick answer instead, see [Reference](../reference).

- [Render Modes](render-modes) — `hybrid` vs `static`, and what actually differs between them.
- [Widgets](widgets) — the built-in `downloads`/`slideshow` widgets, writing your own, and the `!url` tag.
- [Content Toolchain](toolchain) — per-page markdown preprocessing without copy-pasting boilerplate into every file.
- [Publishing](publishing) — `canary publish` and the `publish` config field.
- [Incremental Builds](incremental-builds) — how `canary build`/`canary serve` stay fast without a stale-cache class of bugs.
- [Why Canary](why-canary) — the pitch, and the origin story.
