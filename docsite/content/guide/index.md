# Guide

The concepts behind Canary, not just the commands. If you want a quick answer instead, see [Reference](reference).

- [Render Modes](guide/render-modes) — `hybrid` vs `static`, and what actually differs between them.
- [Widgets](guide/widgets) — the built-in `downloads`/`slideshow`/`code` widgets, writing your own, and the `!url` tag.
- [Content Toolchain](guide/toolchain) — per-page markdown preprocessing without copy-pasting boilerplate into every file.
- [Publishing](guide/publishing) — `canary publish` and the `publish` config field.
- [Incremental Builds](guide/incremental-builds) — always re-render, conditional write, and `canary build --clean` for stale output.
- [Nav and `.nav.json`](guide/nav) — `nav.depth`, hide/allow/deny, `nonav`.
- [Why Canary](guide/why-canary) — the pitch, and the origin story.
